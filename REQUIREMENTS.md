# Desktop Pet — Multiplayer & Chat 확장 요구사항

기존 단일 사용자용 데스크톱 펫(Electron 기반, 투명/클릭통과 창 위에서 캐릭터가 걸어다니는 프로그램)에
**멀티플레이 기능**과 **채팅 기능**을 추가한다. 여러 사용자가 "참여 코드"로 같은 방에 들어가면,
각자의 실제 바탕화면 위에 서로의 캐릭터가 함께 나타나 돌아다니고, 채팅도 할 수 있어야 한다.

> **구현 상태: Phase 1~6 전부 완료.** 이 문서는 최초 기획 원문이라 실제 구현과 다른 부분이
> 몇 군데 있다 — 무엇이 왜 바뀌었는지는 맨 아래 **8. 실제 구현 결과 및 스펙 변경 사항** 참고.
> 프로젝트 전체 개요/실행 방법은 루트 `README.md` 참고.

---

## 1. 핵심 컨셉

- 각 사용자는 자기 컴퓨터에서 프로그램을 실행한다. 화면 공유가 아니라, **각자의 바탕화면 위에 동일한 캐릭터들이 동기화되어 렌더링**되는 방식이다.
- 한 명이 "방 만들기"를 하면 참여 코드(짧은 코드, 예: 6자리 영숫자)가 생성된다.
- 다른 사람은 그 코드를 입력해서 같은 방에 들어간다.
- 방에 들어온 모든 사용자는:
  - 자기 캐릭터가 자기 화면 + 다른 참여자 화면 모두에 나타남
  - 다른 참여자의 캐릭터도 자기 화면에 함께 나타남 (위치/애니메이션 상태 동기화)
  - 캐릭터 근처에 이름표(닉네임) 표시
  - 채팅으로 대화 가능

즉, 서버를 거쳐 "누가 어디에 있고 무슨 상태인지"를 계속 주고받는 실시간 동기화 시스템이 필요하다.

---

## 2. 필요한 구성 요소

### 2.1 클라이언트 (기존 Electron 앱 확장)
- 기존 투명/알웨이즈온탑/클릭통과 캐릭터 창은 유지
- 로컬 캐릭터 1개 + 원격 참여자 캐릭터 N개를 같은 화면 위에 렌더링해야 함
  → 캐릭터 개수만큼 독립된 투명 창을 띄우거나, 화면 전체 크기의 투명 오버레이 창 하나에 여러 캐릭터를 그리는 방식 중 선택 (아래 "설계 이슈" 참고)
- 시그널링 서버와 WebSocket 연결을 유지해 방 참여/퇴장 및 WebRTC 연결 수립(SDP/ICE 교환)을 처리하고, 연결 수립 후에는 각 참여자와 WebRTC DataChannel로 위치 이벤트·채팅을 직접 주고받음
- 채팅 UI(말풍선 + 별도 채팅창/패널) 추가
- 방 생성/참여 코드 입력 화면(런처 UI) 추가 — 캐릭터가 뜨기 전에 먼저 보여지는 로그인성 화면

### 2.2 서버 (신규 — WebRTC 시그널링 전용, 아주 가벼움)

이 프로젝트는 **WebRTC P2P 방식**을 사용한다. 즉, 방 코드를 매칭해주고 최초 연결을 맺어주는 역할만 서버가 하고, 실제 캐릭터 위치 이벤트와 채팅 메시지는 **참여자끼리 직접(P2P)** 주고받는다. 서버를 안 거치기 때문에 트래픽 부담이 거의 없다.

- **시그널링 서버 역할** (Node.js + WebSocket, Render 무료 플랜)
  - 방 생성 시 고유 참여 코드 발급
  - 참여 코드로 들어온 클라이언트끼리 서로를 "찾게" 도와줌
  - WebRTC 연결에 필요한 SDP(offer/answer)와 ICE candidate를 참여자 사이에서 중계 (연결이 맺어지고 나면 이 서버는 더 이상 관여하지 않음)
  - 참여자 접속/퇴장 감지 및 방 정리(cleanup)
  - **실제 위치/채팅 데이터는 이 서버를 거치지 않음** → Render 무료 플랜(15분 비활성 슬립, 30~60초 콜드스타트)이어도 부담 없음. "방 만들기/참여하기" 시 잠깐 대기하는 정도만 감수하면 됨

- **P2P 데이터 채널** (WebRTC DataChannel)
  - 시그널링 서버를 통해 연결이 맺어지면, 이후 캐릭터 이동 이벤트(3.2)와 채팅 메시지(3.3)는 참여자 브라우저/앱끼리 직접 전송
  - 참여 인원이 늘어날수록 각 클라이언트가 나머지 모든 참여자와 개별 연결을 맺는 **메시(mesh) 구조**가 됨 → 인원이 많아지면(8명 이상) 클라이언트 쪽 부담이 커지므로, 최대 인원 제한(3.1)을 넉넉하지 않게 유지하는 게 좋음

- **NAT 통과 보조 서버 (STUN/TURN)**
  - STUN: 구글이 공개로 제공하는 무료 STUN 서버 사용 가능 (`stun:stun.l.google.com:19302` 등) — 대부분의 경우 이것만으로 직접 연결 성사됨
  - TURN: 직접 연결이 실패하는 네트워크(엄격한 방화벽/대칭형 NAT) 대비용 폴백. **Open Relay Project(Metered 제공)**가 월 20GB까지 무료로 TURN 서버를 제공하므로 이걸 사용
    - TURN이 동작하면 그 참여자만 데이터가 TURN 서버를 경유(중계)하게 되며, 이 경우 트래픽이 발생하니 완전한 무료 P2P는 아닐 수 있음을 참고
  - 클라이언트의 `RTCPeerConnection` 설정에 STUN + TURN 서버 목록을 함께 넣어두면, 브라우저/WebRTC 스택이 알아서 가능한 경로 중 최적을 선택함

> **정리**: Render에는 아주 가벼운 시그널링 서버 하나만 올리고, 실제 게임/채팅 트래픽은 참여자 기기끼리 직접 주고받는다. 이렇게 하면 무료 인프라만으로도 충분히 운영 가능하다.

---

## 3. 기능 명세

### 3.1 방(Room) 생성 및 참여
- [x] "방 만들기" 버튼 → 서버에 요청 → 6자리 참여 코드(예: `A3F9K2`) 발급받아 화면에 표시
- [x] "참여하기" 화면 → 코드 입력창 + 닉네임 입력창 → 서버에 참여 요청
- [x] 잘못된 코드 입력 시 에러 안내
- [x] 방장이 나가도 방 자체는 유지(다른 참여자가 있으면), 마지막 인원이 나가면 방 자동 삭제 (단, 유예시간 적용 — 8번 참고)
- [x] 참여 인원 제한 (기본 8명, 환경변수로 조정 가능)

### 3.2 캐릭터 동기화
- **동기화 방식: 이벤트 기반(움직임 의도 전달) — 좌표를 매 프레임 전송하지 않음**
  - 캐릭터는 "목표 지점까지 일정 속도로 걷는다"는 예측 가능한 움직임만 하므로, 매 프레임 좌표를 전송할 필요가 없음
  - 클라이언트는 자신의 움직임 상태가 **바뀌는 시점**(걷기 시작, 방향 전환, 목표 도달 후 정지, idle→walking 전환 등)에만 **WebRTC DataChannel로 다른 참여자들에게 직접** 이벤트를 전송 (시그널링 서버는 거치지 않음):
    - 예: `{ type: 'walk', from: {x, y}, to: {targetX, targetY}, speed, startTime, direction }`
    - 예: `{ type: 'idle', at: {x, y}, startTime }`
  - 이 이벤트는 정규화 좌표(0~1, 화면 크기 대비 비율)로 전송 (3.4 참고)
  - **수신 클라이언트는 이벤트를 받아 로컬에서 직접 애니메이션을 계산**해서 그림 — 로컬 캐릭터의 기존 `walkTo` 로직을 재사용해서, 원격 캐릭터도 매 프레임(예: `requestAnimationFrame`) 자체적으로 위치를 갱신
    → 네트워크 전송 빈도와 무관하게 화면에는 항상 60fps로 부드럽게 그려짐 (끊김/스냅 현상 없음)
  - 네트워크 지연으로 인한 영향은 "다른 사람 화면에서 캐릭터가 몇백ms 늦게 출발"하는 정도로, 위치가 순간이동하듯 끊기는 것보다 훨씬 자연스러움
  - 참고: 만약 나중에 캐릭터 조작을 더 자유롭게(자유 드래그 등) 만들고 싶다면, 그 경우에 한해 소량의 실시간 좌표 스트리밍 + 보간(interpolation) 방식을 별도로 추가 검토
- 클라이언트는 원격 참여자의 이벤트를 받아 자기 화면 위에 해당 캐릭터를 그림 (자기 화면 해상도에 맞춰 좌표 스케일링 필요 — 참여자마다 모니터 해상도가 다를 수 있음)
- 참여자 퇴장 시 해당 캐릭터를 화면에서 제거
- 접속 직후에는 새로 들어온 참여자가 방의 기존 참여자들과 WebRTC 연결을 맺으면서, 각 참여자에게 "현재 상태"(마지막 이벤트)를 한 번씩 요청/수신해서 화면에 바로 나타나게 함 (연결 직후 최초 상태 동기화 메시지 1회)

### 3.3 채팅
- [x] 단축키(조작 모드 중 `Enter`)로 채팅 입력창 활성화 (캐릭터 클릭은 대신 랜덤 대사용으로 남겨둠 — 8번 참고)
- [x] 메시지 전송 시:
  - **WebRTC DataChannel로 방의 다른 참여자들에게 직접 전송** (시그널링 서버는 거치지 않음)
  - 보낸 사람의 캐릭터 위에 말풍선으로 표시 (몇 초 후 자동 사라짐)
  - 별도의 채팅 로그 패널(선택적으로 열고 닫을 수 있는 작은 창)에도 누적 기록
- [x] 닉네임 색상 구분 등으로 누가 말했는지 구분 가능하게

### 3.4 좌표계/화면 크기 차이 처리
- 참여자마다 모니터 해상도가 다르므로, 위치는 절대 픽셀 좌표가 아니라 **화면 크기 대비 비율(0~1 정규화 좌표)**로 주고받고, 각 클라이언트가 자기 화면 크기에 맞춰 실제 픽셀로 변환해서 그려야 함
- 화면 높이가 다른 경우에도 캐릭터가 "바닥(화면 하단)"에 서 있는 것처럼 보이도록 y좌표는 화면 하단 기준으로 계산

### 3.6 조작 모드 (관전 / 조작 / 채팅)

> ⚠️ 아래 표 중 "관전 모드=자율 이동", "방향키(또는 WASD)" 부분은 실제 구현과 다르다 —
> 관전 모드는 자율 이동 없이 정지 상태이고, WASD는 빠졌다(방향키만). 8번 참고.

캐릭터 오버레이는 기본적으로 마우스 클릭이 통과되는 상태(사용자가 다른 앱에서 업무를 계속할 수 있게)이지만, 특정 단축키를 누르면 일시적으로 오버레이가 입력을 받는 모드로 전환되어야 한다. 오버레이 창에 포커스가 없어도(다른 앱 작업 중이어도) 단축키가 항상 동작해야 하므로 **전역 단축키(글로벌 핫키)**로 구현한다.

**모드 정의**

| 모드 | 단축키 | 동작 |
|---|---|---|
| 관전 모드 (기본값) | `Shift + 1` | 오버레이는 완전히 클릭 통과 상태. 자기 캐릭터는 기존처럼 자율적으로 랜덤하게 걸어다님(3.2와 별개로 로컬 자율 이동 로직 유지). 멀티플레이어로 참여 중이면 다른 참여자 캐릭터들도 각자의 자율 이동/이벤트에 따라 화면에 함께 보임. 사용자는 이 캐릭터들을 구경하며 자기 업무(다른 앱)를 그대로 진행 가능 |
| 조작 모드 | `` Shift + ` `` | 오버레이가 키보드 입력을 받는 상태로 전환됨(마우스 클릭 통과는 유지해도 무방). **자기 캐릭터만** 조작 대상이 됨 — 자기 캐릭터의 자율 이동은 멈추고, 방향키(또는 WASD)로 직접 캐릭터를 이동시킬 수 있음. 다른 참여자의 캐릭터는 이 모드에서도 절대 조작할 수 없고, 클릭/드래그 등 어떤 상호작용도 불가능함(관전 전용). 이동 시 발생하는 위치 이벤트는 기존 3.2의 이벤트 기반 동기화 방식과 동일하게 다른 참여자에게 전송(단, "자율 걷기"가 아니라 "사용자 조작에 의한 이동"이라는 것만 다름 — 이벤트 포맷은 동일하게 재사용 가능) |
| 채팅 입력 | 조작 모드 중 `Enter` | 화면 하단에 채팅 입력창이 슬라이드 업으로 나타남. 이 순간에는 키보드 입력이 전부 채팅창으로 감(이동 단축키 비활성화). 메시지 작성 후 `Enter`로 전송, `Esc`로 입력 취소하고 창 닫기. 전송된 메시지는 3.3과 동일하게 처리(말풍선 + 채팅 로그) |
| 모드 종료 | `Esc` 또는 다시 `Shift + 1` | 조작 모드/채팅 입력 상태에서 관전 모드로 복귀. 복귀 시 캐릭터는 다시 자율 이동 로직으로 전환 |

**구현 메모**
- **소유권 원칙 (중요)**: 각 참여자는 자기 캐릭터(자신이 접속하며 생성한 캐릭터 인스턴스)만 조작할 수 있다. 다른 참여자의 캐릭터는 화면에 보이기만 할 뿐, 클릭·드래그·조작 모드에서의 키보드 이동 등 그 어떤 방식으로도 건드릴 수 없다 — 관전 전용(read-only)이다. 이는 UI 단에서만 막는 게 아니라, 이동 이벤트를 만들 때 항상 "발신자 자신의 캐릭터 ID"로만 이벤트를 생성하도록 강제하고, 수신 측에서도 들어온 이벤트의 캐릭터 ID가 그 이벤트를 보낸 참여자 본인의 것인지 검증한 뒤에만 반영해야 한다(다른 사람 ID로 위장한 이동 이벤트를 보내는 것을 막기 위함)
- 단축키는 Electron의 `globalShortcut` 모듈로 등록 (`Shift+1`, `` Shift+` ``, 필요시 `Esc`)해서 오버레이 창이 포커스를 갖지 않은 상태(다른 앱 작업 중)에서도 항상 인식되게 함
- 관전 모드 ↔ 조작 모드 전환 시 창의 `setIgnoreMouseEvents` / 포커스 상태를 함께 토글해야 함:
  - 관전 모드: 클릭 통과 ON, 키보드 입력 캡처 OFF
  - 조작 모드: 키보드 입력 캡처 ON (방향키 등을 오버레이가 받아야 함), 마우스는 계속 통과시켜도 무방(단, 채팅 입력창이 떠 있을 때는 그 입력창 영역만 클릭 가능하게 예외 처리 필요)
- 모드 전환 시 현재 상태를 화면 한쪽에 작게 표시해주면 좋음 (예: 좌상단에 작은 아이콘/텍스트로 "관전 모드" / "조작 모드 (이동: 방향키, 채팅: Enter)" 안내) — 사용자가 지금 어떤 모드인지 헷갈리지 않게
- 조작 모드로 이동 중에도 다른 참여자의 캐릭터는 계속 그들의 자율/조작 이동 이벤트에 따라 정상적으로 화면에 반영되어야 함 (내 모드 전환이 남의 캐릭터 표시에 영향 주면 안 됨)
- 채팅 입력창이 열려 있는 동안에는 이동 단축키(방향키)가 채팅 텍스트 입력으로 흡수되지 않도록 명확히 분리 (입력창에 포커스가 가 있을 때는 방향키가 캐릭터 이동이 아니라 텍스트 커서 이동 등으로 오작동하지 않게 이벤트 핸들러 분리 필요)


### 3.7 프라이버시/보안 (최소한)
- [x] 참여 코드는 추측하기 어렵게 충분히 랜덤해야 함(무작위 대입 방지) — 코드 자체의 랜덤성 + 서버 쪽 join 시도 rate limit까지 반영
- [x] 각 참여자는 자기 캐릭터 ID를 발급받고, 모든 이동/상태 이벤트에 이 ID를 포함해서 전송. 수신 측은 이벤트의 발신자(WebRTC 연결 상대)와 이벤트 안의 캐릭터 ID가 일치하는지 검증한 뒤에만 반영 — 다른 참여자의 캐릭터를 조작하는 위장 이벤트를 막기 위함 (3.6 참고, 실제로는 "메시지 안의 ID 필드 대조"가 아니라 "어느 연결로 들어왔는지"로 신원을 판단하도록 구현 — 8번 참고)
- [x] 서버는 위치/상태/채팅 메시지 외의 개인정보(파일, 클립보드, 화면 내용 등)는 절대 수집/전송하지 않음 — 이 프로그램은 캐릭터 좌표와 채팅 텍스트만 다룬다 (자세한 내용은 `PRIVACY.md` 참고)
- [x] 채팅 메시지 길이 제한, 간단한 스팸/도배 방지(연속 전송 속도 제한) 고려 — 보내는 쪽/받는 쪽 양쪽에 다 적용

---

## 4. 설계 이슈 (구현 전에 결정 필요)

1. **여러 캐릭터를 어떻게 화면에 그릴 것인가?** → ✅ **옵션 A로 확정 및 구현.** 화면 전체 크기의
   투명 오버레이 창 1개(`renderer/index.html`)에 로컬+원격 캐릭터를 전부 DOM으로 그림.

2. **시그널링 서버는 어디서 돌릴 것인가?** → ✅ Render 무료 Web Service로 배포 완료 (2.2, 5, 8 참고)

3. **재연결 처리**: WebRTC 연결이 끊겼을 때(네트워크 순단 등) 자동 재연결 로직, 그리고 시그널링 서버 자체 재연결(콜드스타트 후 등) 처리 필요
   → ⚠️ **미구현 (의도적으로 보류).** 연결이 완전히 끊기면 자동 재시도 없이 캐릭터를 화면에서
   제거하고(짧은 "연결이 끊겼어요" 안내 후), 다시 하려면 참여 코드로 재입장해야 함. 자세한 이유는
   8번 참고.

---

## 5. 서버 배포 (Render — 시그널링 전용)

> ✅ **배포 완료.** `wss://onthedesk-signaling.onrender.com`으로 실제 운영 중 (하위 폴더 `server/`를
> Root Directory로 지정해서 배포 — 별도 레포로 분리하지 않음). 배포 방법은 `server/README.md` 참고.

- 시그널링 서버 코드(Node.js + WebSocket, SDP/ICE 중계 로직만 포함)는 클라이언트(Electron 앱) 레포와 분리된 별도 레포 또는 하위 폴더로 관리
- Render에서 "New Web Service" → GitHub 레포 연결 → 빌드/시작 명령어 지정(예: `npm install` / `npm start`) → 무료 플랜 선택으로 배포
- 배포되면 `https://프로젝트이름.onrender.com` 형태의 URL이 발급됨 → 이 URL을 시그널링 WebSocket 접속 주소(`wss://...`)로 클라이언트에 설정
- 클라이언트에 서버 주소를 하드코딩하지 말고 설정 파일/환경변수로 분리해서, 나중에 다른 호스팅으로 옮길 때 클라이언트 재배포 없이 대응 가능하게 하는 것을 권장
- 환경변수로 관리하면 좋은 값: 포트, 방 최대 인원 수, 방 비활성 정리 시간(cleanup timeout) 등
- STUN 서버 주소(구글 공개 STUN)와 TURN 서버 자격증명(Open Relay Project 발급)은 클라이언트의 `RTCPeerConnection` 설정값으로 별도 관리 (2.2 참고)

## 6. 베이스 기능 (싱글 플레이 — 이 문서의 전제)

아래는 멀티플레이/채팅/조작모드를 얹기 전에 이미 갖춰져 있어야 할 기본 데스크톱 펫 기능이다. 이 문서의 모든 확장 기능(멀티플레이, 채팅, 조작 모드)은 이 기반 위에 추가되는 것을 전제로 한다.

> ⚠️ 아래 두 항목(자율 이동, 드래그)은 실제로는 **제거**했다 — 8번 참고.

- [x] Electron 기반, 화면 전체 크기의 투명·테두리 없는·항상 위(always-on-top) 오버레이 창
- [x] 기본 상태(관전 모드)에서는 마우스 클릭이 오버레이를 통과해서 아래 다른 앱으로 전달됨 (업무 방해 안 함)
- ~~캐릭터는 화면 하단을 기준으로 랜덤하게 좌우로 걸어다님~~ → 제거됨. 평소(관전 모드)엔 화면 정중앙에 가만히 서 있고, 조작 모드에서 방향키로만 움직임(8번 참고)
- ~~캐릭터를 마우스로 드래그해서 원하는 위치로 옮길 수 있음~~ → 제거됨(위와 같은 이유)
- [x] 캐릭터를 클릭하면 랜덤한 짧은 대사가 말풍선으로 표시됨
- [x] 시스템 트레이 아이콘에서 캐릭터 숨기기/보이기, 종료 가능

## 7. 기존 문서와의 관계

이 문서는 위 6번의 베이스 기능(싱글 플레이 데스크톱 펫)을 전제로, 그 위에 방/참여코드 기반 멀티플레이, 채팅, 조작 모드(3.6)를 얹는 확장 스펙이다. 베이스 기능(개인 캐릭터의 자율 이동, 드래그 이동, 클릭 시 랜덤 대사)은 그대로 유지하면서, 멀티플레이 모드에서는 원격 캐릭터 렌더링과 채팅이, 조작 모드에서는 키보드 기반 캐릭터 제어가 추가되는 구조로 설계한다.

---

## 8. 실제 구현 결과 및 스펙 변경 사항

Phase 1~6을 실제로 만들면서, 사용하다 보니 이 문서 원안과 다르게 가는 게 나아 보여서(또는 사용자 요청으로) 바뀐 부분들. 전부 논의를 거쳐 확정한 것이고, 나중에 다시 바꿀 수도 있음.

### 이동 방식 — 자율 이동 · 드래그 제거, 방향키 전용 조작으로 통일
- 관전 모드에서의 랜덤 자율 이동과 마우스 드래그 이동을 **둘 다 제거**했다. 평소엔 화면
  정중앙에 가만히 서 있고, 조작 모드(`` Shift+` ``)에서 방향키로만 움직인다.
- 방향키만 지원하고 **WASD는 뺐다** (필요 없다는 판단).
- 상하좌우 4방향 이동을 지원하며, 이에 맞춰 캐릭터 스프라이트도 4방향(상/하/좌/우)을 다 가진 이미지로 교체했다(아래 참고). 좌우반전(CSS)이 아니라 실제 다른 방향 이미지를 사용한다.
- 캐릭터 시작 위치는 "화면 하단"이 아니라 **화면 정중앙**으로 변경.

### 캐릭터 비주얼
- 원래 계획엔 없던 부분 — 사용자가 직접 만든 16×16 픽셀아트 스프라이트(고양이/강아지/개구리/거북이 4종, 방향별 정지/이동 프레임)로 교체. 런처에서 참여자가 원하는 종을 고를 수 있고, 다른 참여자에게도 그 종으로 보인다(닉네임처럼 시그널링 서버가 같이 중계).
- 캐릭터/말풍선/이름표 크기가 **해상도에 비례해서 조정**된다(3.4의 좌표 정규화와 같은 원리 — 각자 자기 화면 기준으로 계산해서 그림). REQUIREMENTS.md 3.4는 좌표만 언급했지만, 크기도 같은 문제가 있어서 확장 적용함.

### 조작 모드 구현 세부사항
- 오버레이 창(캐릭터가 보이는 창)은 절대 OS 포커스를 갖지 않도록(`focusable:false`) 만들었다.
  이렇게 안 하면 조작 모드 전환 시 macOS에서 화면이 깜빡이는 문제가 있었다. 대신 방향키 입력은
  화면 밖에 떠 있는 별도의 보이지 않는 창이, 채팅 입력은 또 다른 별도의 작은 창이 담당한다.
- `Esc`는 전역 단축키로 등록하지 않았다 — 조작 모드 중엔 위 입력 전용 창이 이미 OS 포커스를
  갖고 있어서 일반 키 이벤트로 충분히 잡히고, 전역으로 등록하면 다른 앱에서의 평범한 Esc
  사용(다이얼로그 닫기 등)까지 가로채는 부작용이 있어서다.
- 조작 모드 중 완전히 다른 앱(다중 모니터에서 다른 모니터를 클릭하는 경우 포함)으로 포커스가
  넘어가면 자동으로 관전 모드로 돌아온다(macOS `did-resign-active` 감지).
- "캐릭터 ID 검증"(3.6/3.7)은 메시지 안의 ID 필드를 대조하는 방식이 아니라, **그 이벤트가
  실제로 어느 WebRTC 연결(DataChannel)로 들어왔는지**로 신원을 판단하도록 구현했다 — 애초에
  다른 사람 ID를 사칭할 방법 자체가 없는 구조.

### 새로 추가된 기능 (원안에 없던 것)
- **다중 모니터 지원**: 모니터가 2대 이상이면 런처에서 오버레이를 띄울 모니터를 고를 수 있다.
  (참고: macOS에서 보조 모니터를 고른 경우 세로 위치가 약 30px 정도 살짝 어긋나는 현상이
  있는데, 원인을 찾아봤지만 Electron/macOS 쪽의 다중 모니터 좌표 처리 특성으로 보이고
  명확한 해결책을 못 찾았다 — 실사용에는 거의 티가 안 나는 수준.)

### 보류한 것
- **재연결 처리(설계 이슈 3번)**: 자동 재연결 로직은 만들지 않았다. 연결이 끊기면 캐릭터가
  화면에서 사라지고(짧은 안내 문구와 함께), 다시 참여하려면 참여 코드로 재입장해야 한다.
  캐주얼한 용도라 완전 자동 재연결의 구현 복잡도 대비 실익이 크지 않다고 판단해서 미룬 것 —
  필요해지면 나중에 추가 가능.

### 문서 오류
- 원안 3번 섹션에 "3.5"는 실제로 존재하지 않는다(3.4 다음이 바로 3.6). Phase 6(프라이버시/보안
  마무리)은 3.7 + 3.6의 소유권 검증 로직 재점검으로 진행했다.
