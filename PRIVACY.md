# 프라이버시 / 보안 요약 (REQUIREMENTS.md 3.7)

이 문서는 "이 앱이 무엇을 주고받는지, 무엇을 안 하는지"를 한 곳에 정리한 것이다.
서버 쪽 세부사항은 `server/README.md`의 "프라이버시/보안" 절 참고.

## 이 앱이 다루는 데이터는 이것뿐이다
1. 캐릭터의 위치/이동 상태 (정규화 좌표, 3.4)
2. 채팅 텍스트
3. 닉네임 (참여자가 직접 입력한 표시용 문자열, 실명/계정 아님)
4. 캐릭터 종류 선택(고양이/강아지/개구리/거북이) — 다른 참여자에게 내 캐릭터를 어떤 모양으로
   그릴지 알려주기 위해 닉네임과 함께 전달됨

이 외의 어떤 것도(파일, 클립보드, 화면 내용, 다른 앱 정보 등) 수집하거나 전송하지 않는다.

**참고**: 어느 모니터에 오버레이를 띄울지(다중 모니터 선택)는 순전히 내 컴퓨터 안에서만
쓰이는 로컬 설정이라, 서버나 다른 참여자에게 전혀 전달되지 않는다.

## 어디를 거치고, 어디는 안 거치는가
- **시그널링 서버(Render)**: 방 코드 발급/참여, WebRTC 연결에 필요한 SDP·ICE 중계까지만
  관여한다. 연결이 맺어진 뒤에는 서버가 더 이상 끼어들지 않는다.
- **캐릭터 위치 이벤트와 채팅 메시지는 서버를 절대 거치지 않는다** — 참여자 브라우저(앱)
  끼리 WebRTC DataChannel로 직접 주고받는다(P2P). 서버 로그에도 남지 않는다.
- TURN 서버를 실제로 경유하게 되는 예외적인 네트워크 상황(엄격한 NAT/방화벽)에서는
  그 참여자의 트래픽이 TURN 중계 서버를 지나가지만, 이 역시 서버가 내용을 해석/저장하지
  않고 그대로 전달만 한다.

## 다른 참여자 캐릭터를 위장/조작할 수 없는 이유 (3.6/3.7)
- 각 참여자는 시그널링 서버로부터 무작위 참여자 ID를 받고, 그 ID로 다른 참여자와
  1:1 WebRTC 연결(DataChannel)을 맺는다.
- 수신 클라이언트는 "이 이동/채팅 이벤트가 어떤 참여자의 것인가"를 메시지 안에 적힌
  자기 신고 값으로 판단하지 않는다. **그 이벤트가 실제로 어느 DataChannel(=어느 참여자와의
  연결)로 들어왔는지**로만 판단한다. 따라서 다른 참여자의 ID를 사칭한 이벤트를 만들 방법이
  프로토콜 구조상 없다.
- 시그널링 단계에서도 동일한 원칙이 적용된다 — SDP/ICE 중계 메시지의 `from` 필드는
  클라이언트가 보낸 값이 아니라 서버가 실제 연결 기준으로 채운다.
- 로컬 UI 차원에서도, 원격 참여자의 캐릭터는 클릭/드래그 등 어떤 마우스 상호작용도
  받지 않도록 만들어져 있다(관전 전용). 자기 캐릭터만 조작 가능하다(3.6).

## 참여 코드 보호
- 참여 코드는 헷갈리는 문자(`0/O`, `1/I/L`)를 뺀 32종 문자 조합 6자리를, 암호학적으로
  안전한 난수(`crypto.randomInt`)로 생성한다.
- 같은 IP에서 짧은 시간에 너무 많은 참여 시도를 하면 서버가 잠깐 차단한다(무작위 대입 방지).

## 채팅 스팸/도배 방지
- 보내는 쪽: 메시지 길이 최대 200자, 최소 전송 간격 0.5초로 제한.
- 받는 쪽: 상대가 자기 클라이언트의 제한을 무시하고 보내더라도, 너무 빠른 연속 메시지는
  조용히 무시한다(최소 수신 간격 0.3초).

## 로컬에 저장되는 것
- 마지막으로 입력한 닉네임, 마지막으로 고른 캐릭터 종류, 마지막으로 고른 모니터 —
  다음 실행 때 다시 안 고르고 편하게 쓰려고 사용자 데이터 폴더에 저장 (전부 이 컴퓨터 안에만
  있고 다른 곳으로 전송되지 않음)
- 그 외에는 아무것도 로컬에 저장하지 않는다 (채팅 로그도 실행 중에만 메모리에 있고,
  앱을 재시작하면 사라짐)
